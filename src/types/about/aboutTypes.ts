export type ExperienceData = {
    position: string,
    company : string,
    start_date: string,
    end_date: string | null,
    work_status: boolean,
}

export type SkillData = {
    label: string
}